# Uncomment the next line to define a global platform for your project
 platform :ios, '9.0'
 use_frameworks!

targetArray = ['GPUImage的使用', '其他滤镜']

targetArray.each do |t|
  
    target t do
      pod 'GPUImage'
    end

end
